---
name: tailor_resume
description: Automatically tailor a resume for a job from the queue where tailored docs are missing or generated by a local LLM. Updates HTML dashboard automatically.
---

When invoked, follow these exact steps to tailor a resume using your AI reasoning capabilities:

1. **Fetch a Job**
   - Run the command: `venv\Scripts\python.exe scratch\manage_tailor_queue.py 1` (cwd: `c:\Users\vinee\manju_jobs`).
   - Read the file `c:\Users\vinee\manju_jobs\scratch\agent_batch.json`.
   - If the file is missing or contains an empty array `[]`, STOP immediately and output: "Queue is empty."

2. **Read Job Details and Template**
   - Extract the `job_id`, `title`, `company`, and `description` from the job in the batch.
   - Read the resume template from: `c:\Users\vinee\Manju_jobs_private\Resumes\f6aaa66f\f6aaa66f_data.json`.
   - Use the following Candidate Profile for Manju:
     ```
     - LL.M. Business & Corporate Law, First Rank — Symbiosis International University (2020–21)
     - LL.B. First Class Honours, Top 3 — University of Calicut (2009–14)
     - Finnish Supplementary Law Studies (OPH bar path) — University of Lapland (2025–present)
     - Kohti Yliopistoa — University of Oulu (2025–26)
     - Language placement: Asianajajatoimisto Regelin Oy, Oulu (Apr–Jun 2026)
     - Intern: International House Oulu — 14 events, OuluBot (Jan–Apr 2025, Sep–Oct 2024)
     - Legal Associate: Poise Legal India (Oct 2021–May 2022) — 5–7 contracts/month
     - Junior Lawyer: Juris Nexus India (Sep 2015–Jan 2016) — family & civil law
     - Finnish B2, English C1, Malayalam native. Based in Oulu. Available Sep 2026.
     ```

3. **Tailor the Resume (YOU DO THIS)**
   - Use your AI intelligence to generate a new JSON file matching the template structure EXACTLY.
   - `profile`: 2-3 sentences HIGHLY tailored to the specific role and company.
   - `cover_letter.paragraphs`: 4-5 persuasive paragraphs tailored to the company and role. Write in Finnish if the job description is in Finnish, otherwise English.
   - Ensure you replace all instances of the template company ("Hiab") with the new company's name.
   - Update `job_id`, `job_title`, and `company` keys.
   - Set `"tailored_at": "<current_iso_time>"` (e.g. `"2026-07-16T12:00:00+03:00"`) representing the exact time you are running this skill.
   - Set `"tailor_model": "Antigravity IDE"` in the new JSON.

4. **Save the Tailored JSON**
   - Use `write_to_file` to save your generated JSON to: `c:\Users\vinee\Manju_jobs_private\Resumes\[job_id]\[job_id]_data.json`. (Make sure to create the parent directory if it does not exist).

5. **Update jobs.json**
   - Run the following Python snippet (e.g. by saving to a temp file and running it) to update `jobs.json` with the new tailor model:
     ```python
     import json
     import sys
     job_id = sys.argv[1]
     with open(r'c:\Users\vinee\manju_jobs\jobs.json', 'r', encoding='utf-8') as f:
         jobs = json.load(f)
     for j in jobs:
         if j.get('id') == job_id:
             j['tailor_model'] = 'Gemini 3.1 Pro'
             break
     with open(r'c:\Users\vinee\manju_jobs\jobs.json', 'w', encoding='utf-8') as f:
         json.dump(jobs, f, indent=2, ensure_ascii=False)
     ```
     Run this script with `job_id` as the argument.

6. **Run the Pipeline**
   - Run the command: `powershell -ExecutionPolicy Bypass -Command "& 'venv\Scripts\Activate.ps1'; .\run_make_resume_pipeline.ps1 [job_id]"` (cwd: `c:\Users\vinee\manju_jobs`), replacing `[job_id]` with the actual job_id.
   - This executes the pipeline to build the PDFs, commit to GitHub, and sync the links to Firestore, automatically updating the HTML dashboard.
   - Inform the user that the pipeline completed successfully.
